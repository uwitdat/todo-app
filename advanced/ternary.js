const myAge = 17
let message

myAge >= 18 ? message = 'You may vote' : message = 'you may not vote sorry'


console.log(message)

const team = [
    'tyler',
    'porter',
    'tyler',
    'porter',
    'chris'
]


res = team.length <= 4 ? `Team size: ${team.length}` : 'too many people on your team'

console.log(res)